generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlserver"
  url      = env("DATABASE_URL")
}

model employee {
  MATRIC        Int        @id @default(autoincrement())
  CRACHA        String     @unique
  FUNCIONARIO   String
}

model historyAddress {
  id      Int   @id @default(autoincrement())
  odfNumber             String 
}